package sound;

import com.pupmod.pupmod;

import net.minecraft.block.SoundType;
import net.minecraft.util.SoundEvent;
import net.minecraft.util.SoundEvents;
import net.minecraftforge.registries.DeferredRegister;
import net.minecraftforge.registries.ForgeRegistries;

public class soundtype extends SoundType{
	
	   public soundtype(float f, float g, SoundEvent event, SoundEvent eventi,
			SoundEvent eventii, SoundEvent ievent, SoundEvent iievent) {
		super(f, g, event, eventi, eventii, ievent, iievent);
	}
	   
	 //  1.0F, 1.0F, soundregistry.NIGGISOUND.get(), soundregistry.NIGGISOUND.get(), soundregistry.NIGGISOUND.get(), soundregistry.NIGGISOUND.get(), soundregistry.NIGGISOUND.get()

}
